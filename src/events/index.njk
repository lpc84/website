---
pagination:
    data: events_list
    size: 2
    alias: posts
permalink: "/events/"
layout: dawn/default.njk
---
<div class="row">
    <h2>Próximos Eventos</h2>
</div>
<div class="row">
    {% for post in posts[0] %}
        <section style="border: .5px solid var(--darker-gray-color);border-radius: 1em;" class="column" class="single-header kg-canvas">
            <a href="/events/{{post.title | slug}}" style="width:100%">
                <div style="margin-bottom:1em;margin-top:0;" class="single-media kg-width-wide u-placeholder">
                    {%if post.banner[0].url %}
                        <div class="u-placeholder horizontal">
                            {% image post.banner[0].url, post.title, [357, 600, 972], ["event-picture"], ["u-object-fit", "event-photo"] %}
                        </div>
                        {%endif%}

                    </div>
                    <div style="margin-left: 2em">
                        <h4 style="margin-bottom: .3em">{{post | eventDate}} | {{post.local}} | {{post.type}}</h4>
                        <h3>{{post.title}}</h3>
                    </div>
                </a>
            </section>

        {% endfor %}
    </div>
    <div class="single-content kg-canvas u-text-format"/>
        <p></p>
        <h3>Tem um evento que deseja partilhar com a rede? Preencha <a href="/events/new-event/">este formulário</a> que nós revemos e divulgamos</h3>
        <p></p>
</div>
<div class="row">
    <h2>Eventos Passados</h2>
</div>
<div class="row">
    {% for post in posts[1] %}
        <section style="border: .5px solid var(--darker-gray-color);border-radius: 1em;" class="column" class="single-header kg-canvas">
            <a href="/events/{{post.title | slug}}" style="width:100%">
                <div style="margin-bottom:1em;margin-top:0;" class="single-media kg-width-wide u-placeholder">
                    {%if post.banner[0].url %}
                        <div class="u-placeholder horizontal">
                            {% image post.banner[0].url, post.title, [357, 600, 972], ["event-picture"], ["u-object-fit", "event-photo"] %}
                        </div>
                        {%endif%}

                    </div>
                    <div style="margin-left: 2em">
                        <h4 style="margin-bottom: .3em">{{post | eventDate}} | {{post.local}} | {{post.type}}</h4>
                        <h3>{{post.title}}</h3>
                    </div>
                </a>
            </section>

        {% endfor %}
    </div>
<style>
    .row {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: space-between;
        width: 100%;
        max-width: 1130px;
        margin: 1em auto;
    }

    .column {
        display: flex;
        flex-direction: column;
        flex-basis: 48%;
        width: 50%;
    }

    .event-picture {
        width:100%;
    }
    
    @media (max-width: 536px) {
        .column {
            align-items: center;
            flex-basis: 95%;
            margin: 1em;
            width: 100%;
        }
    }
</style>